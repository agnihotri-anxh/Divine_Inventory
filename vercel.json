{
  "buildCommand": "pnpm --filter @workspace/inventory-app run build && rm -rf public && cp -r artifacts/inventory-app/dist/public public",
  "installCommand": "pnpm install",
  "outputDirectory": "public",
  "cleanUrls": true,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
